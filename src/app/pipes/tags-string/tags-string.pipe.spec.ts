import { TagsStringPipe } from './tags-string.pipe';

describe('TagsStringPipe', () => {
  it('create an instance', () => {
    const pipe = new TagsStringPipe();
    expect(pipe).toBeTruthy();
  });
});
