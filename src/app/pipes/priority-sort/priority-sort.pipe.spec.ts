import { PrioritySortPipe } from './priority-sort.pipe';

describe('PrioritySortPipe', () => {
  it('create an instance', () => {
    const pipe = new PrioritySortPipe();
    expect(pipe).toBeTruthy();
  });
});
